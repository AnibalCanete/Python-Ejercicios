
# Creamos el diccionario (d), con sus respectivos valores
d = {
    "A": 1,
    "B": 2
}

# Con la función (values) traemos los valores de las claves del diccionario y con la función (list) lo convertimos en una lista, y lo imprimimpos
print(list(d.values())) 
