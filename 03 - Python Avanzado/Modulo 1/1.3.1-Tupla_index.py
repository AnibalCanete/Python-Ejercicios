
# Con 1 Parametro
l = (7, 7, 7, 3, 5) # Creamos la variable (l) con sus respectivos elementos
print(l.index(5)) # Con la función (index()) podemos indicar en que posición se encuentra el elemento que buscamos

# Con 2 Parametros. Indicar desde que indice partir la búsqueda
l = (7, 7, 7, 3, 5) # Creamos la variable (l) con sus respectivos elementos
print(l.index(7, 2)) # El segundo parametro es el que indica desde que elemento inicia la busqueda
