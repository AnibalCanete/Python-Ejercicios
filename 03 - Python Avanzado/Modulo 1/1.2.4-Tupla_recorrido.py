
tupla = 1, 2, 3, "Soy un string", 5, 18.5 # Creamos la variable (tupla) con sus respectivos elementos
for i in tupla: # Con el (for), utilizamos la variable (i) para recorrer la variable (tupla)
    print(i) # Imprimimos los elementos de la variable (i)

