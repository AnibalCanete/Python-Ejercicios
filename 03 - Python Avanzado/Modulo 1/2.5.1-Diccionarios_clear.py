
# Creamos el diccionario (d), con sus respectivos valores
d = {
    "A": 1,
    "B": 2
}

print(d) # Imprimimos el diccionario antes de limpiarlo
d.clear() # Con la función (clear()), podemos limpiar el diccionario
print(d) # Imprimimos el diccionario despues de limpiarlo
