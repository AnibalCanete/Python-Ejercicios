
# For con String/Cadena de Caracteres
# Declaramos la variable (texto), que es un texto escrito
texto = "Hola, Mundo"

# Con el (for) con la variable (x) recorremos los elementos de la variable (texto)
for x in texto:
    print(x) # Imprimimos la variable (x)
    