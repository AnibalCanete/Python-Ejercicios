
lista = [1, 2, 3] # Creamos la variable (lista)
tupla = tuple(lista) # Creamos la variable (tupla) y con la función (tuple) convertimos la variable (lista) en una tupla
print(type(tupla)) # Imprimimos el tipo de dato de la variable (tupla)
print(tupla) # Imprimimos la variable (tupla)
