
l = (1, 2, 3) # Creamos la Tupla (l) con sus respectivos elementos
x, y, z = l # Creamos tres variables (x, y, z) y le asignamos a cada variable un elemento de la tupla
print(x, y, z) # Imprimimos las variables (x, y, z)
