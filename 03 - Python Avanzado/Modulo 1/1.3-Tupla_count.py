
l = (1, 1, 1, 3, 5) # Creamos la variable (tupla), con sus respectivos elementos
print(l.count(1)) # Con la función (count()), imprimimos la cantidad de números repetidos en la tupla
print(l.count(10)) # Si un elemento no existe en la tupla nos dara como resultado 0
